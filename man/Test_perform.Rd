% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Test_perform.R
\name{Test_perform}
\alias{Test_perform}
\title{Evaluate model performance on testing data}
\usage{
Test_perform(
  outcome.type,
  outcome,
  survdays = NULL,
  time_point = NULL,
  train_data,
  test_data,
  test_outcome,
  test_time = NULL,
  features,
  models.indiv,
  ensemble = FALSE,
  models.ens = NULL,
  param.indiv = NULL,
  param.ens = NULL,
  auto.tune = FALSE,
  tune.method = c("random", "grid", "successive_halving", "hyperband", "bayes"),
  tune.n = 5,
  tune.folds = 3,
  tune.iter = 1,
  tune.models = NULL
)
}
\arguments{
\item{outcome.type}{Character string specifying the prediction task. Must be one of \code{"survival"}, \code{"binary"}, or \code{"categorical"} corresponding to survival analysis, binary classification, and multi-class classification.}

\item{outcome}{Outcome vector for the training data. For \code{"binary"} and \code{"survival"} outcomes, this should be a binary vector (0/1). For \code{"categorical"} outcomes, this should be a numeric or factor vector representing class labels.}

\item{survdays}{Numeric vector of survival times for the training data, required when \code{outcome.type = "survival"}.}

\item{time_point}{Numeric value specifying the time point at which survival predictions are evaluated.}

\item{train_data}{training data}

\item{test_data}{testing data}

\item{test_outcome}{Outcome vector for testing data. For \code{"binary"} and \code{"survival"} outcomes, this should be a binary vector (0/1). For \code{"categorical"} outcomes, this should be a numeric or factor vector representing class labels.}

\item{test_time}{Survival times for testing data, required when \code{outcome.type = "survival"}.}

\item{features}{Feature specification. For pathway-based modeling this should be a list of pathways where each element is a character vector of genes belonging to that pathway. For protein-level modeling this should be a character vector of proteins used as features.}

\item{models.indiv}{Character vector specifying base machine learning or deep learning models used for individual model training. Supported abbreviations include: \code{"XG"} (xgboost), \code{"RF"} (random forest), \code{"EN"} (elastic net), \code{"GB"} (gradient boosting), \code{"KNN"} (k-nearest neighbors), \code{"SVM"} (support vector machine), \code{"NB"} (naive Bayes), \code{"DCT"} (decision tree), \code{"NN"} (neural network), \code{"ADB"} (AdaBoost), and \code{"MB"} (model-based boosting).}

\item{ensemble}{Logical. If \code{TRUE}, ensemble (stacking) prediction modeling is performed.}

\item{models.ens}{Character vector specifying models used to construct the stacking/ensemble model. Model abbreviations follow the same convention as \code{models.indiv}.}

\item{param.indiv}{Named list of user-specified parameters for the individual base models listed in \code{models.indiv}. If \code{NULL}, default parameters appropriate for the specified outcome type are used.}

\item{param.ens}{Named list of user-specified parameters for ensemble model training. If \code{NULL}, default parameters are used.}
}
\description{
This function evaluates the predictive performance of trained models
on an independent testing dataset using a fixed parameter configuration
(no cross-validation). Performance metrics are computed for each
specified base model and, if enabled, the ensemble model. The results
provide an unbiased assessment of out-of-sample generalization.
}
\examples{
\dontrun{
# -----------------------------------------------------------------------
# Independent test-set evaluation
# -----------------------------------------------------------------------
data(caspen_example)

data(caspen_test_example)

train_x <- caspen_example$data_to_pass
train_y <- caspen_example$outcome
test_x <- caspen_test_example$test_data
test_y <- caspen_test_example$test_outcome

path_res <- pathway_select(
  outcome.type = "binary",
  outcome = train_y,
  data = train_x,
  features = caspen_example$pathways[1:10],
  models.indiv = c("RF", "EN", "SVM"),
  iter = 5,
  num.folds = 3,
  fs.AUC.cut = 0.70,
  fs.sp.cut = 0.10
)

selected_features <- path_res$features

test_res <- Test_perform(
  outcome.type = "binary",
  outcome = train_y,
  train_data = train_x,
  test_data = test_x,
  test_outcome = test_y,
  features = selected_features,
  models.indiv = c("RF", "EN", "SVM"),
  ensemble = TRUE,
  models.ens = c("RF", "EN", "SVM")
)

test_res$auc
test_res$SP.95

# For survival, also supply survival time and time_point.
set.seed(2)
survival_time <- sample(120:1800, nrow(caspen_example$data_to_pass),
                        replace = TRUE)
survival_status <- caspen_example$outcome
test_survival_time <- sample(120:1800, nrow(caspen_test_example$test_data),
                             replace = TRUE)

surv_test_res <- Test_perform(
  outcome.type = "survival",
  outcome = survival_status,
  survdays = survival_time,
  time_point = 365,
  train_data = train_x,
  test_data = test_x,
  test_outcome = test_y,
  test_time = test_survival_time,
  features = selected_features,
  models.indiv = c("RF", "EN", "DCT"),
  ensemble = TRUE,
  models.ens = c("RF", "EN")
)

surv_test_res$C.index
surv_test_res$SP.95
}

}
