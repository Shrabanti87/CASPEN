% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_level_performance.R
\name{Gene_Test_perform}
\alias{Gene_Test_perform}
\title{Evaluate gene-level model performance on independent testing data}
\usage{
Gene_Test_perform(
  outcome.type,
  outcome,
  survdays = NULL,
  time_point = NULL,
  train_data,
  test_data,
  test_outcome,
  test_time = NULL,
  genes = NULL,
  models.indiv = NULL,
  ensemble = TRUE,
  models.ens = NULL,
  param.indiv = NULL,
  param.ens = NULL,
  auto.tune = FALSE,
  tune.method = c("random", "grid", "successive_halving", "hyperband", "bayes"),
  tune.n = 5,
  tune.folds = 3,
  tune.iter = 1,
  tune.models = NULL
)
}
\arguments{
\item{outcome.type}{Character string specifying the prediction task. Must be
one of \code{"binary"}, \code{"categorical"}, \code{"survival"}, or \code{"continuous"}.}

\item{outcome}{Outcome vector for the training data.}

\item{survdays}{Numeric vector of training survival times, required when
\code{outcome.type = "survival"}.}

\item{time_point}{Numeric value specifying the time point at which survival
time-dependent AUC and SP95 are evaluated.}

\item{train_data}{Training data matrix or data frame.}

\item{test_data}{Independent test data matrix or data frame.}

\item{test_outcome}{Outcome vector for the test data.}

\item{test_time}{Survival times for the test data, required when
\code{outcome.type = "survival"}.}

\item{genes}{Optional character vector of genes/proteins/features to use.
If \code{NULL}, all shared columns between \code{train_data} and \code{test_data} are used.}

\item{models.indiv}{Character vector specifying base models. If \code{NULL}, all
models supported by the selected outcome type are used.}

\item{ensemble}{Logical. If \code{TRUE}, fit stacked ensemble models. Defaults to
\code{TRUE} for gene-level performance because there is no pathway block and the
aggregation is over base learners.}

\item{models.ens}{Character vector specifying ensemble learners.}

\item{param.indiv}{Named list of model parameters for base models.}

\item{param.ens}{Named list of model parameters for ensemble models.}

\item{auto.tune}{Logical. If \code{TRUE}, tune base-model parameters on the
training gene matrix before independent test evaluation.}

\item{tune.method, tune.n, tune.folds, tune.iter, tune.models}{Tuning controls.}
}
\value{
A data frame of model-level independent test performance metrics.
}
\description{
\code{Gene_Test_perform()} fits CASPEN models directly on a gene/protein feature
matrix and evaluates them on an independent test set. There is no pathway
loop. Base learners are fit on the selected genes, and their fitted
prediction columns are used by the stacked ensemble learners. With \code{ensemble = TRUE}, returned rows are the requested ensemble learners plus simple and
weighted average ensembles; with \code{ensemble = FALSE}, returned rows are the
individual base learners.
}
\examples{
\dontrun{
data(caspen_example)
data(caspen_test_example)

genes <- colnames(caspen_example$data_to_pass)[1:40]
gene_test <- Gene_Test_perform(
  outcome.type = "binary",
  outcome = caspen_example$outcome,
  train_data = caspen_example$data_to_pass,
  test_data = caspen_test_example$test_data,
  test_outcome = caspen_test_example$test_outcome,
  genes = genes,
  models.indiv = c("RF", "EN", "XG"),
  ensemble = TRUE,
  models.ens = c("RF", "EN", "XG")
)

gene_test$auc
gene_test$SP.95
}

}
