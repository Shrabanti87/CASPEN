% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_pathway_selection}
\alias{plot_pathway_selection}
\title{Plot Pathway Selection Performance}
\usage{
plot_pathway_selection(
  x,
  outcome.type = c("auto", "binary", "categorical", "survival"),
  model = NULL,
  label.top = 8,
  main = NULL,
  xlab = "Sensitivity at 95\% specificity",
  ylab = NULL,
  selected.color = "#1f7a3d",
  ...
)
}
\arguments{
\item{x}{A result object from \code{pathway_select()}, \code{Train_perform_CV()}, or
\code{Test_perform()} containing \code{auc} or \code{C.index}/\code{cindex} and \code{SP.95}/\code{sp.95}.}

\item{outcome.type}{One of \code{"auto"}, \code{"binary"}, \code{"categorical"}, or
\code{"survival"}. \code{"auto"} uses C-index if present.}

\item{model}{Optional model column/name to plot. If \code{NULL}, row means across
all available models are used.}

\item{label.top}{Number of top-performing points to label.}

\item{main}{Optional plot title.}

\item{xlab}{Optional x-axis label.}

\item{ylab}{Optional y-axis label.}

\item{selected.color}{Point color.}

\item{...}{Additional arguments passed to \code{plot()}.}
}
\value{
Invisibly returns the plotted data frame.
}
\description{
Draws a scatter plot of pathway-level prediction performance against
sensitivity at 95 percent specificity. For survival results, the y-axis uses
C-index when available; otherwise it uses AUC.
}
\examples{
\dontrun{
data(caspen_example)

path_res <- pathway_select(
  outcome.type = "binary",
  outcome = caspen_example$outcome,
  data = caspen_example$data_to_pass,
  features = caspen_example$pathways[1:5],
  models.indiv = c("RF", "EN"),
  iter = 5,
  num.folds = 3
)

plot_pathway_selection(path_res, outcome.type = "binary", label.top = 5)

# For survival pathway_select() results, the y-axis becomes C-index.
plot_pathway_selection(path_res, outcome.type = "survival", model = "RF")
}

}
