% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_roc_curve}
\alias{plot_roc_curve}
\title{Plot ROC Curves}
\usage{
plot_roc_curve(
  x,
  outcome = NULL,
  outcome.type = c("binary", "categorical", "survival"),
  survdays = NULL,
  time.point = NULL,
  model = NULL,
  main = NULL,
  ...
)
}
\arguments{
\item{x}{A pROC ROC object, survivalROC object, list of ROC objects, or a
numeric prediction vector/matrix.}

\item{outcome}{Binary/categorical outcome vector. Required when \code{x} contains
predictions rather than ROC objects.}

\item{outcome.type}{One of \code{"binary"}, \code{"categorical"}, or \code{"survival"}.}

\item{survdays}{Survival time vector for \code{outcome.type = "survival"}.}

\item{time.point}{Prediction time for survival ROC.}

\item{model}{Optional model name when \code{x} is a named list.}

\item{main}{Optional plot title.}

\item{...}{Additional plotting arguments.}
}
\value{
Invisibly returns ROC objects used for plotting.
}
\description{
Plots binary, multiclass, or survival ROC curves from CASPEN ROC objects or
prediction scores. \code{Train_perform_CV()} and \code{Test_perform()} summary tables
include AUC/SP95, while lower-level ROC helper results may include \code{roc} and
\code{predictions} elements that can be passed directly to this function.
}
\examples{
\dontrun{
data(caspen_example)

# Plot from a numeric risk/probability vector.
marker <- caspen_example$data_to_pass[, 1]
plot_roc_curve(
  x = marker,
  outcome = caspen_example$outcome,
  outcome.type = "binary",
  main = "Example binary ROC"
)

# Survival ROC at one year from a numeric risk score.
set.seed(1)
survival_time <- sample(120:1800, nrow(caspen_example$data_to_pass),
                        replace = TRUE)
survival_status <- caspen_example$outcome

plot_roc_curve(
  x = marker,
  outcome = survival_status,
  outcome.type = "survival",
  survdays = survival_time,
  time.point = 365,
  main = "Example one-year survival ROC"
)
}

}
