% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_level_performance.R
\name{Gene_Train_perform_CV}
\alias{Gene_Train_perform_CV}
\title{Evaluate gene-level model performance on training data by cross-validation}
\usage{
Gene_Train_perform_CV(
  outcome.type,
  outcome,
  survdays = NULL,
  time_point = NULL,
  train_data,
  genes = NULL,
  models.indiv = NULL,
  iter,
  num.folds = NULL,
  ensemble = TRUE,
  models.ens = NULL,
  param.indiv = NULL,
  param.ens = NULL,
  auto.tune = FALSE,
  tune.method = c("random", "grid", "successive_halving", "hyperband", "bayes"),
  tune.n = 5,
  tune.folds = 3,
  tune.iter = 1,
  tune.models = NULL,
  parallel.iter = FALSE,
  workers = future::availableCores(),
  future.seed = NULL,
  future.strategy = "multisession"
)
}
\arguments{
\item{outcome.type}{Character string specifying the prediction task. Must be
one of \code{"binary"}, \code{"categorical"}, \code{"survival"}, or \code{"continuous"}.}

\item{outcome}{Outcome vector for the training data.}

\item{survdays}{Numeric vector of survival times, required when
\code{outcome.type = "survival"}.}

\item{time_point}{Numeric value specifying the time point at which survival
time-dependent AUC and SP95 are evaluated.}

\item{train_data}{Training data matrix or data frame with samples in rows and
genes/proteins/features in columns.}

\item{genes}{Optional character vector of genes/proteins/features to use.
If \code{NULL}, all columns of \code{train_data} are used.}

\item{models.indiv}{Character vector specifying base models. If \code{NULL}, all
models supported by the selected outcome type are used.}

\item{iter}{Integer number of repeated cross-validation iterations.}

\item{num.folds}{Integer number of cross-validation folds. Default is 3.}

\item{ensemble}{Logical. If \code{TRUE}, fit stacked ensemble models. Defaults to
\code{TRUE} for gene-level performance because there is no pathway block and the
aggregation is over base learners.}

\item{models.ens}{Character vector specifying ensemble learners.}

\item{param.indiv}{Named list of model parameters for base models.}

\item{param.ens}{Named list of model parameters for ensemble models.}

\item{auto.tune}{Logical. If \code{TRUE}, tune base-model parameters on the gene
matrix before performance evaluation.}

\item{tune.method, tune.n, tune.folds, tune.iter, tune.models}{Tuning controls.}

\item{parallel.iter}{Logical; if \code{TRUE}, repeated iterations are split across
a temporary \code{future} backend.}

\item{workers}{Integer number of future workers. Defaults to available cores.}

\item{future.seed}{Passed to \code{future.apply::future_lapply()}.}

\item{future.strategy}{Future backend strategy used when \code{parallel.iter = TRUE}.}
}
\value{
A data frame of model-level performance metrics. For binary and
categorical outcomes, AUC and SP95 are returned. For survival outcomes,
C-index, time-dependent AUC, and SP95 are returned. For continuous outcomes,
C-index, R2, MAE, and RMSE are returned when available.
}
\description{
\code{Gene_Train_perform_CV()} fits CASPEN models directly on a gene/protein
feature matrix instead of looping over pathways. In each fold, base learners
are fit on the selected genes and their predictions become the inputs to the
stacked ensemble learners. Predictions are aggregated over cross-validation
folds and repeated iterations. With \code{ensemble = TRUE}, returned rows are the
requested ensemble learners plus simple and weighted average ensembles; with
\code{ensemble = FALSE}, returned rows are the individual base learners.
}
\examples{
\dontrun{
data(caspen_example)

gene_train <- Gene_Train_perform_CV(
  outcome.type = "binary",
  outcome = caspen_example$outcome,
  train_data = caspen_example$data_to_pass,
  genes = colnames(caspen_example$data_to_pass)[1:40],
  models.indiv = c("RF", "EN", "XG"),
  iter = 5,
  num.folds = 3,
  ensemble = TRUE,
  models.ens = c("RF", "EN", "XG")
)

gene_train$auc
gene_train$SP.95
}

}
